let keystone = require('keystone')
let Promise = require('bluebird')

let validateTest = (model, cms = false) => {
  return new Promise((resolve, reject) => {
    resolve()
  })
}

module.exports = validateTest
