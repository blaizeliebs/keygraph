let keystone = require('keystone')
let Promise = require('bluebird')

let validatePerson = (model, cms = false) => {
  return new Promise((resolve, reject) => {
    resolve()
  })
}

module.exports = validatePerson
